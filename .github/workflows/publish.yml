name: Publish

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
      - name: Upgrade npm to latest (required for OIDC)
        run: npm install -g npm@latest
      - name: Debug OIDC environment
        run: |
          echo "npm version: $(npm --version)"
          echo "Node version: $(node --version)"
          echo "OIDC token request URL exists: $([[ -n \"$ACTIONS_ID_TOKEN_REQUEST_URL\" ]] && echo 'YES' || echo 'NO')"
          echo "OIDC token request token exists: $([[ -n \"$ACTIONS_ID_TOKEN_REQUEST_TOKEN\" ]] && echo 'YES' || echo 'NO')"
          npm config list
      - name: Install dependencies
        run: bun install
      - name: Build
        run: bun run build
      - name: Publish to NPM
        run: npm publish --access public --provenance
        env:
          NPM_CONFIG_PROVENANCE: true
